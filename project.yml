name: Zenith
options:
  bundleIdPrefix: com.anuraag
targets:
  Zenith:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources: [ContentView.swift, ZenithApp.swift, DesignSystem, Models, ViewModels, Components, Views]
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.anuraag.Zenith
